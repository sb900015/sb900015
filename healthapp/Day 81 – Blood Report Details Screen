# 🚀 Day 81/90 – Blood Report Details Screen

Congratulations! 🎉

You've now built:

* ✅ Add reports
* ✅ View all reports
* ✅ Delete reports
* ✅ Search reports

Today you'll add a **Report Details Screen**.

Instead of only showing:

```text
Alice
Hemoglobin : 13.5
```

users will be able to tap the report and see all its information on a separate page.

---

# Today's Goal

```text
All Reports

Alice
Hemoglobin : 13.5
        ↓ Tap

-----------------------
Report Details

Patient Name : Alice

Hemoglobin : 13.5

Report ID : 4
-----------------------
```

---

# Step 1

## Create a new file

```
lib/screens/report_details_screen.dart
```

Paste this **entire file**:

```dart
import 'package:flutter/material.dart';

class ReportDetailsScreen extends StatelessWidget {

  final Map report;

  const ReportDetailsScreen({
    super.key,
    required this.report,
  });

  @override
  Widget build(BuildContext context) {

    return Scaffold(

      appBar: AppBar(
        title: const Text("Report Details"),
      ),

      body: Padding(

        padding: const EdgeInsets.all(20),

        child: Card(

          elevation: 5,

          child: Padding(

            padding: const EdgeInsets.all(20),

            child: Column(

              crossAxisAlignment: CrossAxisAlignment.start,

              children: [

                const Text(

                  "Patient Information",

                  style: TextStyle(

                    fontSize: 24,

                    fontWeight: FontWeight.bold,

                  ),

                ),

                const SizedBox(height: 25),

                Text(
                  "Report ID : ${report["id"]}",
                  style: const TextStyle(fontSize: 20),
                ),

                const SizedBox(height: 20),

                Text(
                  "Patient Name : ${report["patient_name"]}",
                  style: const TextStyle(fontSize: 20),
                ),

                const SizedBox(height: 20),

                Text(
                  "Hemoglobin : ${report["hemoglobin"]}",
                  style: const TextStyle(fontSize: 20),
                ),

                const SizedBox(height: 30),

                const Divider(),

                const SizedBox(height: 20),

                const Text(

                  "More blood parameters will be displayed here in future.",

                  style: TextStyle(

                    fontSize: 18,

                    color: Colors.grey,

                  ),

                ),

              ],

            ),

          ),

        ),

      ),

    );

  }

}
```

---

# Step 2

Replace your entire

```
lib/screens/all_reports_screen.dart
```

with this complete file:

```dart
import 'package:flutter/material.dart';

import '../api/api_service.dart';
import 'report_details_screen.dart';

class AllReportsScreen extends StatefulWidget {

  const AllReportsScreen({super.key});

  @override
  State<AllReportsScreen> createState() =>
      _AllReportsScreenState();

}

class _AllReportsScreenState
    extends State<AllReportsScreen> {

  List reports = [];

  List filteredReports = [];

  bool loading = true;

  @override
  void initState() {

    super.initState();

    loadReports();

  }

  Future<void> loadReports() async {

    setState(() {
      loading = true;
    });

    ApiService api = ApiService();

    reports = await api.getReports();

    filteredReports = List.from(reports);

    setState(() {
      loading = false;
    });

  }

  void searchReports(String value) {

    setState(() {

      if (value.isEmpty) {

        filteredReports = List.from(reports);

      } else {

        filteredReports = reports.where((report) {

          return report["patient_name"]
              .toString()
              .toLowerCase()
              .contains(value.toLowerCase());

        }).toList();

      }

    });

  }

  Future<void> confirmDelete(int id) async {

    bool? delete = await showDialog(

      context: context,

      builder: (context) {

        return AlertDialog(

          title: const Text("Delete Report"),

          content: const Text(
              "Are you sure you want to delete this report?"),

          actions: [

            TextButton(

              onPressed: () {

                Navigator.pop(context, false);

              },

              child: const Text("No"),

            ),

            ElevatedButton(

              onPressed: () {

                Navigator.pop(context, true);

              },

              child: const Text("Yes"),

            ),

          ],

        );

      },

    );

    if (delete == true) {

      ApiService api = ApiService();

      await api.deleteReport(id);

      loadReports();

    }

  }

  @override
  Widget build(BuildContext context) {

    return Scaffold(

      appBar: AppBar(

        title: const Text("Blood Reports"),

      ),

      body: loading

          ? const Center(
              child: CircularProgressIndicator(),
            )

          : Column(

              children: [

                Padding(

                  padding: const EdgeInsets.all(12),

                  child: TextField(

                    onChanged: searchReports,

                    decoration: InputDecoration(

                      hintText: "Search Patient",

                      prefixIcon: const Icon(Icons.search),

                      border: OutlineInputBorder(

                        borderRadius:
                            BorderRadius.circular(10),

                      ),

                    ),

                  ),

                ),

                Expanded(

                  child: ListView.builder(

                    itemCount: filteredReports.length,

                    itemBuilder: (context, index) {

                      return Card(

                        margin: const EdgeInsets.symmetric(

                          horizontal: 10,

                          vertical: 5,

                        ),

                        child: ListTile(

                          leading: const Icon(Icons.person),

                          title: Text(
                            filteredReports[index]["patient_name"],
                          ),

                          subtitle: Text(
                            "Hemoglobin : ${filteredReports[index]["hemoglobin"]}",
                          ),

                          onTap: () {

                            Navigator.push(

                              context,

                              MaterialPageRoute(

                                builder: (_) => ReportDetailsScreen(

                                  report: filteredReports[index],

                                ),

                              ),

                            );

                          },

                          trailing: IconButton(

                            icon: const Icon(

                              Icons.delete,

                              color: Colors.red,

                            ),

                            onPressed: () {

                              confirmDelete(
                                filteredReports[index]["id"],
                              );

                            },

                          ),

                        ),

                      );

                    },

                  ),

                ),

              ],

            ),

    );

  }

}
```

---

# Step 3

Run:

```bash
flutter run -d chrome
```

---

# Test

Click any report.

Example:

```
Alice
```

You should now see:

```
Report Details

Report ID : 3

Patient Name : Alice

Hemoglobin : 13.5
```

---

# 📝 Notes for Your Notebook

## Day 81 – Report Details Screen

### Topics Learned

* Navigation using `Navigator.push()`
* Passing data between screens
* Creating a reusable details page
* Using `StatelessWidget` for displaying data

### Key Lesson

> Instead of trying to show everything on one screen, Flutter apps often navigate to a dedicated details page and pass the selected object to it.

---

## 🎯 Day 82 Preview

Tomorrow we'll make the Report Details screen look much more professional by adding:

* Better cards
* Colored status badges
* Icons
* Cleaner spacing
* A polished layout

From Day 82 onward, the app will start looking much closer to a production-quality application.
