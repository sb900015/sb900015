Welcome to **Day 94**! 🎉

You've completed the basic navigation flow:

* ✅ Splash Screen
* ✅ Login
* ✅ Register
* ✅ Home
* ✅ Logout
* ✅ Upload Report
* ✅ Dashboard
* ✅ My Reports

Now let's make the app feel more polished.

---

# Day 94 – Create a Profile Screen

**Time:** ~1 hour

## Goal

Add a **Profile Screen** where users can see their information.

For now, we'll use sample data. Later, we'll load the logged-in user's details from the backend.

---

# Step 1

Create a new file:

```text
lib/screens/profile_screen.dart
```

Replace the **entire file** with:

```dart
import 'package:flutter/material.dart';

class ProfileScreen extends StatelessWidget {
  const ProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("My Profile"),
        centerTitle: true,
      ),
      body: SingleChildScrollView(
        child: Padding(
          padding: const EdgeInsets.all(20),
          child: Column(
            children: [

              const SizedBox(height: 20),

              const CircleAvatar(
                radius: 60,
                child: Icon(
                  Icons.person,
                  size: 70,
                ),
              ),

              const SizedBox(height: 20),

              const Text(
                "Srujan Bharthipudi",
                style: TextStyle(
                  fontSize: 24,
                  fontWeight: FontWeight.bold,
                ),
              ),

              const SizedBox(height: 10),

              const Text(
                "srujan@email.com",
                style: TextStyle(
                  fontSize: 18,
                  color: Colors.grey,
                ),
              ),

              const SizedBox(height: 30),

              Card(
                child: ListTile(
                  leading: const Icon(Icons.favorite),
                  title: const Text("Blood Group"),
                  trailing: const Text("B+"),
                ),
              ),

              Card(
                child: ListTile(
                  leading: const Icon(Icons.cake),
                  title: const Text("Age"),
                  trailing: const Text("35"),
                ),
              ),

              Card(
                child: ListTile(
                  leading: const Icon(Icons.monitor_weight),
                  title: const Text("Weight"),
                  trailing: const Text("86 kg"),
                ),
              ),

              Card(
                child: ListTile(
                  leading: const Icon(Icons.height),
                  title: const Text("Height"),
                  trailing: const Text("171 cm"),
                ),
              ),

              const SizedBox(height: 25),

              SizedBox(
                width: double.infinity,
                height: 55,
                child: ElevatedButton.icon(
                  icon: const Icon(Icons.edit),
                  label: const Text(
                    "Edit Profile",
                    style: TextStyle(fontSize: 18),
                  ),
                  onPressed: () {
                    ScaffoldMessenger.of(context).showSnackBar(
                      const SnackBar(
                        content: Text(
                          "Edit Profile will be added later",
                        ),
                      ),
                    );
                  },
                ),
              ),

            ],
          ),
        ),
      ),
    );
  }
}
```

---

# Step 2

Replace your **entire** `lib/screens/home_screen.dart` with this version:

```dart
import 'package:flutter/material.dart';

import 'all_reports_screen.dart';
import 'login_screen.dart';
import 'post_test_screen.dart';
import 'profile_screen.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  Widget buildButton(
    BuildContext context,
    IconData icon,
    String title,
    Widget page,
    Color color,
  ) {
    return SizedBox(
      width: double.infinity,
      height: 70,
      child: ElevatedButton.icon(
        icon: Icon(icon, size: 30),
        label: Text(
          title,
          style: const TextStyle(fontSize: 20),
        ),
        style: ElevatedButton.styleFrom(
          backgroundColor: color,
          foregroundColor: Colors.white,
        ),
        onPressed: () {
          Navigator.push(
            context,
            MaterialPageRoute(
              builder: (_) => page,
            ),
          );
        },
      ),
    );
  }

  @override
  Widget build(BuildContext context) {

    return Scaffold(

      appBar: AppBar(

        title: const Text("Blood Report App"),

        centerTitle: true,

        actions: [

          IconButton(

            icon: const Icon(Icons.person),

            onPressed: () {

              Navigator.push(

                context,

                MaterialPageRoute(

                  builder: (_) => const ProfileScreen(),

                ),

              );

            },

          ),

          IconButton(

            icon: const Icon(Icons.logout),

            onPressed: () async {

              bool? logout = await showDialog(

                context: context,

                builder: (_) {

                  return AlertDialog(

                    title: const Text("Logout"),

                    content: const Text(
                      "Are you sure you want to logout?",
                    ),

                    actions: [

                      TextButton(

                        onPressed: () {
                          Navigator.pop(context, false);
                        },

                        child: const Text("No"),

                      ),

                      ElevatedButton(

                        onPressed: () {
                          Navigator.pop(context, true);
                        },

                        child: const Text("Yes"),

                      ),

                    ],

                  );

                },

              );

              if (logout == true) {

                Navigator.pushAndRemoveUntil(

                  context,

                  MaterialPageRoute(
                    builder: (_) => const LoginScreen(),
                  ),

                  (route) => false,

                );

              }

            },

          ),

        ],

      ),

      body: Padding(

        padding: const EdgeInsets.all(20),

        child: Column(

          children: [

            const SizedBox(height: 20),

            buildButton(
              context,
              Icons.upload_file,
              "Upload Report",
              const PostTestScreen(),
              Colors.blue,
            ),

            const SizedBox(height: 20),

            buildButton(
              context,
              Icons.description,
              "My Reports",
              const AllReportsScreen(),
              Colors.green,
            ),

            const SizedBox(height: 20),

            buildButton(
              context,
              Icons.dashboard,
              "Dashboard",
              const AllReportsScreen(),
              Colors.orange,
            ),

            const SizedBox(height: 20),

            buildButton(
              context,
              Icons.settings,
              "Settings",
              const Placeholder(),
              Colors.grey,
            ),

          ],

        ),

      ),

    );

  }

}
```

---

# Step 3

Run:

```bash
flutter run
```

or

```bash
flutter build apk
```

Install the updated APK.

---

# Expected Result

The Home Screen app bar will now show:

```
Blood Report App

👤 🚪
```

* Tap **👤** → Opens the Profile Screen.
* Tap **🚪** → Opens the Logout confirmation dialog.

---

# Day 94 Learning

Today you'll learn:

* Creating a Profile Screen
* `CircleAvatar`
* `Card`
* `ListTile`
* AppBar action buttons
* Better screen organization

---

# Progress

Your app now includes:

```
Splash
   ↓
Login
   ↓
Register
   ↓
Home
 ├── Upload Report
 ├── My Reports
 ├── Dashboard
 ├── Profile
 ├── Settings
 └── Logout
```

This is another step toward a complete mobile application. In the next phase, we'll begin replacing the sample profile and authentication data with real backend APIs so users can create accounts and manage their own information.
